#pragma once

//Prótipo das funções

//Para biblioteca de PACOTE
unsigned int empacotar(unsigned short, unsigned short, unsigned short, unsigned short, unsigned short);
unsigned short passo(unsigned int);
unsigned short cor(unsigned int);
unsigned short posicao(unsigned int);
unsigned short velocidade(unsigned int);
unsigned short pista(unsigned int);
